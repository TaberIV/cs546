const fileData = require("./fileData");
const textMetrics = require("./textMetrics");

async function main() {
	const numChapters = 3
	for (var i = 0; i < numChapters; i++) {
		const path = `chapter${i + 1}.txt`;
		console.log(path);

		
	}
}

main();